{
  "name": "jquery-plugins",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "concat": "concat-glob-cli -f ./src/**/*.js -o dist/jquery.plugins.js",
    "uglify": "uglifyjs dist/jquery.plugins.js -o dist/jquery.plugins.min.js",
    "build": "npm run concat && npm run uglify"
  },
  "keywords": [],
  "author": "olivmonnier",
  "license": "ISC",
  "devDependencies": {
    "concat-glob-cli": "^0.1.0",
    "uglify-js": "^3.5.13"
  }
}
